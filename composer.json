{
    "name": "zagrev/dmbc-tools",
    "description": "Shared tools for the Dayton Metro Barbershop Chorus WordPress site.",
    "type": "wordpress-plugin",
    "require": {},
    "require-dev": {
        "phpunit/phpunit": "^9.6"
    },
    "config": {
        "vendor-dir": "vendor"
    },
    "autoload": {
        "classmap": [
            "includes/"
        ]
    },
    "scripts": {
        "test": "phpunit",
        "lint": "php -l dmbc-tools.php && php -l includes/class-plugin.php"
    }
}
