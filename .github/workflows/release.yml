name: Release

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    types:
      - closed

jobs:
  release:
    if: github.event_name == 'workflow_dispatch' || github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout main
        uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0

      - name: Set release version
        id: version
        run: |
          CURRENT_VERSION=$(sed -nE 's/^ \* Version: ([0-9]+\.[0-9]+\.[0-9]+)$/\1/p' dmbc-tools.php)
          test -n "$CURRENT_VERSION"

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="$CURRENT_VERSION"
          else
            LATEST_TAG=$(git tag --list 'v[0-9]*' --sort=-version:refname | head -n 1)
            LATEST_VERSION="${LATEST_TAG#v}"
            if [ -z "$LATEST_TAG" ]; then
              LATEST_VERSION="$CURRENT_VERSION"
            fi
            IFS=. read -r MAJOR MINOR PATCH <<< "$LATEST_VERSION"
            VERSION="$MAJOR.$MINOR.$((PATCH + 1))"

            sed -i -E "s/^ \* Version: .*/ * Version: ${VERSION}/" dmbc-tools.php
            sed -i -E "s/(public const string VERSION[[:space:]]*=[[:space:]]*')[^']+('.*)/\1${VERSION}\2/" src/plugin.php
            sed -i -E "s/^Stable tag: .*/Stable tag: ${VERSION}/" readme.txt
          fi

          grep -F "Version: $VERSION" dmbc-tools.php
          grep -F "public const string VERSION" src/plugin.php | grep -F "'$VERSION'"
          grep -F "Stable tag: $VERSION" readme.txt
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Commit and tag release version
        if: github.event_name != 'workflow_dispatch'
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dmbc-tools.php src/plugin.php readme.txt
          git commit -m "chore: release v${VERSION}"
          git tag "v${VERSION}"
          git push origin main "v${VERSION}"

      - name: Ensure manual release tag
        if: github.event_name == 'workflow_dispatch'
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          if git ls-remote --exit-code --tags origin "v${VERSION}"; then
            exit 0
          fi
          git tag "v${VERSION}"
          git push origin "v${VERSION}"

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          tools: composer

      - name: Install dependencies
        run: composer install --no-dev --no-progress --prefer-dist --no-interaction

      - name: Create plugin artifact
        id: package
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          test -f vendor/autoload.php
          PACKAGE_DIR="$RUNNER_TEMP/dmbc-tools"
          PACKAGE_ZIP="$RUNNER_TEMP/dmbc-tools-${VERSION}.zip"
          mkdir "$PACKAGE_DIR"
          rsync -a ./ "$PACKAGE_DIR/" \
            --exclude='.git/' \
            --exclude='.github/' \
            --exclude='.phpunit.cache/' \
            --exclude='tests/' \
            --exclude='.wp-env.json' \
            --exclude='composer.json' \
            --exclude='composer.lock' \
            --exclude='phpcs.xml' \
            --exclude='phpunit.xml'
          cd "$RUNNER_TEMP"
          zip -r "$PACKAGE_ZIP" "dmbc-tools"
          sha256sum "$PACKAGE_ZIP"
          echo "path=$PACKAGE_ZIP" >> "$GITHUB_OUTPUT"

      - name: Upload plugin artifact
        uses: actions/upload-artifact@v7
        with:
          name: dmbc-tools-${{ steps.version.outputs.version }}
          path: ${{ steps.package.outputs.path }}
          archive: false

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: v${{ steps.version.outputs.version }}
          generate_release_notes: true
          files: ${{ steps.package.outputs.path }}
